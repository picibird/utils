{
  "name": "@picibird/utils",
  "version": "1.0.0",
  "description": "utils for cloud native stacks",
  "repository": {
    "url": "https://github.com/picibird/utils"
  },
  "license": "ISC",
  "author": "Marcus Specht <me@masp.io> (https://picibird.com)",
  "private": true,
  "workspaces": [
    "./src/*"
  ],
  "scripts": {
    "dev": "yarn ws dev",
    "build": "yarn ws build",
    "test": "yarn ws test",
    "ws": "yarn workspaces foreach -pt run dev",
    "check-env": "node -e 'console.log(process.env)' | grep npm",
    "g:tsc": "cd $INIT_CWD && tsc --tsconfig=${PROJECT_CWD}/.ts-config.json"
  },
  "devDependencies": {
    "typescript": "4.3.2"
  },
  "engines": {
    "node": ">=14 <15"
  }
}
