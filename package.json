{
  "name": "@picibird/utils",
  "version": "1.0.0",
  "description": "utils for cloud native stacks",
  "repository": {
    "url": "https://github.com/picibird/utils"
  },
  "license": "ISC",
  "author": "Marcus Specht <me@masp.io> (https://picibird.com)",
  "private": true,
  "workspaces": [
    "./src/cli",
    "./src/client",
    "./src/server",
    "./src/util"
  ],
  "scripts": {
    "dev": "yarn ws-run dev",
    "build": "yarn ws-run build",
    "test": "yarn ws-run test",
    "start": "yarn ws-run start",
    "ws-run": "yarn workspaces run",
    "check-env": "node -e 'console.log(process.env)' | grep npm",
    "g:tsc": "cd $INIT_CWD && tsc --tsconfig=${PROJECT_CWD}/.ts-config.json"
  },
  "engines": {
    "node": ">=14 <15"
  }
}
